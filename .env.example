# Backend security
API_TOKEN=change-me

# Postgres (compose defaults)
DB_HOST=postgres
DB_PORT=5432
DB_USER=amadeus
DB_PASSWORD=amadeus
DB_NAME=amadeus

# Crypto for key vault (Fernet base64 key). For prod, generate once and set here.
ENCRYPTION_KEY=

# Frontend runtime (optional)
# window.__API__ / window.__WS__ можно задать через index.html или прокинуть reverse-proxy
